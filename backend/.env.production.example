# Copy this to .env.production and fill in your actual values
# Then load with PowerShell:
# Get-Content .env.production | ForEach-Object { if ($_ -match '^([^=]+)=(.*)$') { Set-Item -Path "env:$($matches[1])" -Value $matches[2] } }

OPENAI_API_KEY=sk-proj-your-key-here
AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=https;AccountName=stasiaspaneldev;AccountKey=...
AZURE_TTS_CONTAINER=tts-audio
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
