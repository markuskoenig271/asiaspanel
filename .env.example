# Copy this file to .env and fill in secrets. Do NOT commit .env to git.
# OpenAI
OPENAI_API_KEY=sk-REPLACE_WITH_YOUR_KEY
OPENAI_MODEL=gpt-4

# Optional: model for OpenAI TTS (if available in your account)
OPENAI_TTS_MODEL=gpt-4o-mini-tts

# Azure Storage (use Azurite for local dev or real storage connection string in production)
# Example Azurite (default) connection string (replace <key> with the full key Azurite provides):
# AZURE_STORAGE_CONNECTION_STRING=DefaultEndpointsProtocol=http;AccountName=devstoreaccount1;AccountKey=Eby8vdM02xNOcqFlq...==;BlobEndpoint=http://127.0.0.1:10000/devstoreaccount1;
AZURE_STORAGE_CONNECTION_STRING=

# Container name used for TTS blob uploads (optional)
AZURE_TTS_CONTAINER=tts-audio
